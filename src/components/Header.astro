<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link
  href="https://fonts.googleapis.com/css2?family=Lacquer&display=swap"
  rel="stylesheet"
/>

<div class="containerb">
  <div class="dobleb">
    <div class="bangerv">
      BANGER
      <div class="bangerb">
        BANGER
        <div class="productor">productor musical</div>
      </div>
    </div>
  </div>
</div>

<div class="nav">
  <div class="container">
    <div class="links">
      <a href="#sobremi">Sobre mi</a>
      <span></span>
      <a href="/carreras">producciones</a>
      <span></span>
      <a href="/academica">biblioteca</a>
      <span></span>
      <a href="/extension">suscripciones</a>
      <span></span>
      <a href="/bienestar">contacto</a>
      <span></span>
    </div>
  </div>

  <div class="hamburger">
    <span class="line"></span>
    <span class="line"></span>
    <span class="line"></span>
  </div>

  <script>
    // Obtén el elemento del menú
const links = document.querySelector(".links") as HTMLElement;
const hamburger = document.querySelector(".hamburger") as HTMLElement;

// Añade el manejador de eventos solo si 'hamburger' no es null
if (hamburger) {
    hamburger.addEventListener("click", () => {
        if (links) {
            if (links.classList.contains("expanded")) {
                links.classList.remove("expanded");
                links.style.display = "none"; // Oculta el menú en pantallas grandes
            } else {
                links.classList.add("expanded");
                links.style.display = "flex"; // Muestra el menú en pantallas pequeñas
            }
        }
    });
}

// Ejecuta la función inicial solo si 'links' no es null
if (links) {
    if (links.classList.contains("expanded")) {
        links.style.display = "flex"; // Muestra el menú en pantallas pequeñas
    } else {
        links.style.display = "none"; // Oculta el menú en pantallas grandes
    }
}

</script>

  <style>
    body {
      justify-content: center;
      align-items: center;
    }
    .containerb {
      display: flex;
      height: 37rem;
      background: black;
      justify-content: center;
      align-items: center;
    }
    .dobleb {
      background-color: rgb(0, 0, 0);
      justify-content: center;
    }
    .bangerv {
      display: flex;
      color: rgba(202, 29, 164, 1);
      text-align: center;
      font-family: Lacquer, sans-serif;
      position: relative;
      font-size: 10rem;
      justify-content: center;
    }
    .bangerb {
      display: flex;
      color: white;
      text-align: center;
      position: absolute;
      font-weight: bold;
      font-size: 9rem;
      font-family: system-ui;
      align-items: flex-end;
      justify-content: center;
    }

    .productor {
      color: white;
      text-align: center;
      position: absolute;
      font-size: 1.3rem;
      font-family: auto;
    }

    .nav {
      display: block;
      list-style: none;
      justify-content: center;
      flex-wrap: wrap;
      margin: 0 auto;
    }

    .hamburger {
      display: block;
      position: absolute;
      left: 1rem;
      top: 1.3rem;
      cursor: pointer;
    }

    .hamburger .line {
      display: block;
      width: 40px;
      height: 5px;
      margin-bottom: 10px;
      background-color: white;
      border-radius: 3rem;
    }
    .expanded {
      display: unset;
      display: flex;
      flex-direction: column;
      position: absolute;
      left: 0rem;
      top: 4.3rem;
      background-color: rgba(33, 33, 33, 1);
      font-family: sans-serif;
    }
    .expanded span {
      width: 8rem;
      height: 0.09rem;
      margin-bottom: 10px;
      background-color: white;
      border-radius: 1rem;
    }

    .links a {
      font-size: 1rem;
      text-decoration: none;
      color: white;
      text-transform: uppercase;
      padding: 1rem;
      gap: 1rem;
    }

    .links a:hover {
      color: rgba(202, 29, 164, 1);
    }
    .links a:active {
      color: rgba(202, 29, 164, 1);
    }

    .links a:after {
      border-bottom: 1rem solid white;
    }

    .links {
      display: none;
      align-items: center;
    }
  </style>
</div>
